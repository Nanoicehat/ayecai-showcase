## Why

文件浏览器方便点选，但已知路径时输入更快。提供两种模式让用户自选。

## What Changes

ImportDialog 导入对话框新增切换：
- 模式 A：文件浏览器（现有）
- 模式 B：直接输入绝对路径

用户可通过 tab 切换两种方式，选中任一方式确定文件后均可导入。

## Capabilities

### New Capabilities
- `import-path-input`: 导入对话框支持手动输入绝对路径模式
